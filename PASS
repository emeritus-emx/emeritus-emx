wwef5ttop@1E
cd ~/project/NUESA\ SCHOLARS\ \&\ INTENT

# 1. Add only the modified frontend/auth files (don't add backend deletions)
git add views/Auth.tsx views/Admin.tsx components/Navbar.tsx services/apiService.ts package.json package-lock.json App.tsx

# 2. Commit with a clear message
git commit -m "feat: Add backend authentication validation and password show/hide toggle

- Integrate backend login API verification (prevent arbitrary email login)
- Add show/hide password toggle to login and registration forms
- Fix login flow to validate against backend before creating local session
- Import Eye/EyeOff icons from lucide-react for UI toggle"

# 3. Push to origin
git push origin master